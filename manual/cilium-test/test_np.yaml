apiVersion: v1
kind: Service
metadata:
  labels:
    run: test-nginx
  name: test-nginx
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 80
  selector:
    run: test-nginx
  type: NodePort
status:
  loadBalancer: {}
